(function(){
  'use strict';
  // Vector 5250 is an independent system of record and is intentionally
  // not injected into the Warehouse Management application anymore.
  // Its standalone interface is served at /vector5250 and /vector-5250.
})();
