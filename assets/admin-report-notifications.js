(() => {
  const POLL_MS = 5000;
  const STORAGE_KEY = 'ugamap_admin_seen_report_ids_v1';
  let initialized = false;
  let seen = new Set();
  try { seen = new Set(JSON.parse(localStorage.getItem(STORAGE_KEY) || '[]')); } catch (_) {}
  function saveSeen(){ try{ localStorage.setItem(STORAGE_KEY,JSON.stringify(Array.from(seen).slice(-500))); }catch(_){} }
  function esc(v){ return String(v==null?'':v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])); }
  function reportsTab(){ return document.querySelector('.tab[data-tab="reports"]'); }
  function ensureBadge(){ const tab=reportsTab(); if(!tab)return null; let b=tab.querySelector('.reportNotifyBadge'); if(!b){ b=document.createElement('span'); b.className='reportNotifyBadge'; b.style.cssText='float:right;min-width:20px;padding:2px 6px;border-radius:999px;background:#e2593a;color:#fff;font-size:10px;font-weight:800;text-align:center;display:none'; tab.appendChild(b); tab.addEventListener('click',()=>{b.style.display='none';b.textContent='';}); } return b; }
  function notify(r,count){ const category=String(r.category||'report').replace(/_/g,' '),note=String(r.note||'').trim(),message=`New ${category} report${note?': '+note:''}`; if(typeof window.showNotifyToast==='function')window.showNotifyToast(message,false); const badge=ensureBadge(); if(badge){badge.textContent=String(count);badge.style.display='inline-block';} if('Notification'in window&&Notification.permission==='granted'){try{new Notification('UGAMAP — New Report',{body:message});}catch(_){}} }
  function mediaCell(r){ if(!r.media_url)return '<span style="color:#70768d">—</span>'; const url=esc(r.media_url); if(String(r.media_type||'').startsWith('video/'))return `<video src="${url}" controls playsinline style="width:110px;max-height:90px;border-radius:8px;background:#000"></video>`; return `<a href="${url}" target="_blank" rel="noopener"><img src="${url}" alt="Report photo" style="width:90px;height:70px;object-fit:cover;border-radius:8px;border:1px solid #2a3a50"></a>`; }
  function mapLink(r){ const lat=Number(r.lat),lon=Number(r.lon); if(!Number.isFinite(lat)||!Number.isFinite(lon))return '—'; return `<a class="viewLink" target="_blank" rel="noopener" href="/?report_lat=${encodeURIComponent(lat)}&report_lon=${encodeURIComponent(lon)}">View on Map</a>`; }
  function statusBadge(s){ const v=String(s||'new').toLowerCase(); const bg=v==='resolved'?'#d4f4dd':v==='reviewed'?'#dbeafe':'#fde8d4'; const fg=v==='resolved'?'#1a7a37':v==='reviewed'?'#1d4ed8':'#a35b00'; return `<span style="display:inline-block;padding:4px 8px;border-radius:7px;background:${bg};color:${fg};font-weight:800;font-size:11px">${esc(v.toUpperCase())}</span>`; }
  async function setStatus(id,status){ try{ const headers=typeof window.authHeaders==='function'?window.authHeaders():{}; const res=await fetch('/admin/reports/'+encodeURIComponent(id)+'/status',{method:'POST',headers:{...headers,'Content-Type':'application/x-www-form-urlencoded'},body:new URLSearchParams({status})}); const d=await res.json().catch(()=>({})); if(!res.ok)throw new Error(d.detail||('HTTP '+res.status)); if(typeof window.showNotifyToast==='function')window.showNotifyToast('Report marked '+status,false); await window.loadReports(); }catch(e){ if(typeof window.showNotifyToast==='function')window.showNotifyToast('Unable to update report: '+e.message,true); } }
  window.ugamapSetReportStatus=setStatus;
  function actions(r){ const s=String(r.status||'new').toLowerCase(); const id=JSON.stringify(String(r.id||'')); if(s==='new')return `<button class="rowBtn edit" onclick='ugamapSetReportStatus(${id},"reviewed")'>Review</button><button class="rowBtn delete" onclick='ugamapSetReportStatus(${id},"resolved")'>Resolve</button>`; if(s==='reviewed')return `<button class="rowBtn delete" onclick='ugamapSetReportStatus(${id},"resolved")'>Resolve</button><button class="rowBtn toggle" onclick='ugamapSetReportStatus(${id},"new")'>Reopen</button>`; return `<button class="rowBtn toggle" onclick='ugamapSetReportStatus(${id},"reviewed")'>Reopen</button>`; }
  function renderAdminReports(rows){ const out=document.getElementById('reportsOut'); if(!out)return; if(!rows.length){out.innerHTML='<div class="empty">No reports yet.</div>';return;} out.innerHTML=`<div style="overflow-x:auto"><table><thead><tr><th>Category</th><th>Note</th><th>Media</th><th>Location</th><th>Time</th><th>Status</th><th>Actions</th><th>Map</th></tr></thead><tbody>${rows.slice().sort((a,b)=>Number(b.created_at||0)-Number(a.created_at||0)).map(r=>{const lat=Number(r.lat),lon=Number(r.lon);const location=Number.isFinite(lat)&&Number.isFinite(lon)?`${lat.toFixed(4)}, ${lon.toFixed(4)}`:'—';const when=r.created_at?new Date(Number(r.created_at)*1000).toLocaleString():'—';return `<tr><td>${esc(String(r.category||'').replace(/_/g,' '))}</td><td>${esc(r.note||'')}</td><td>${mediaCell(r)}</td><td>${location}</td><td>${esc(when)}</td><td>${statusBadge(r.status)}</td><td>${actions(r)}</td><td>${mapLink(r)}</td></tr>`;}).join('')}</tbody></table></div>`; }
  window.loadReports=async function(){ const out=document.getElementById('reportsOut'); try{const res=await fetch('/core/reports',{cache:'no-store'});if(!res.ok)throw new Error('HTTP '+res.status);const d=await res.json();renderAdminReports(Array.isArray(d.results)?d.results:[]);}catch(e){if(out)out.innerHTML='<div class="empty">Error loading reports.</div>';} };
  async function poll(){ try{const res=await fetch('/core/reports',{cache:'no-store'});if(!res.ok)return;const d=await res.json(),rows=Array.isArray(d.results)?d.results:[];if(!initialized){rows.forEach(r=>r.id&&seen.add(r.id));saveSeen();initialized=true;return;}const fresh=rows.filter(r=>r.id&&!seen.has(r.id));if(!fresh.length)return;fresh.forEach(r=>seen.add(r.id));saveSeen();notify(fresh[fresh.length-1],fresh.length);window.loadReports();}catch(_){} }
  document.addEventListener('DOMContentLoaded',()=>{ensureBadge();if('Notification'in window&&Notification.permission==='default'){document.addEventListener('click',()=>Notification.requestPermission().catch(()=>{}),{once:true});}poll();setInterval(poll,POLL_MS);});
})();
