FROM python:3.12-slim

WORKDIR /app

COPY ddr_radar_server.py /app/ddr_radar_server.py

RUN pip install --no-cache-dir fastapi uvicorn pydantic

EXPOSE 8080

CMD ["sh", "-c", "uvicorn ddr_radar_server:app --host 0.0.0.0 --port ${PORT:-8080}"]
